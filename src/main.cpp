#include <iostream>
#include "utils.h"

int main() {
    int x = 5, y = 3;

    std::cout << "Multiply: " << multiply(x, y) << std::endl;
    std::cout << "Add: " << add(x, y) << std::endl;
    return 0;
}